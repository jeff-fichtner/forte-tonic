/**
 * Trimester constants for frontend
 * Mirrors backend enum in src/utils/values/trimester.js
 */
export const Trimester = {
  FALL: 'fall',
  WINTER: 'winter',
  SPRING: 'spring',
};

/**
 * Array of trimesters in chronological order
 */
export const TRIMESTER_SEQUENCE = Object.values(Trimester);
