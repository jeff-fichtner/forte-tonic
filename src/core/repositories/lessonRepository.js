/*

const addLesson =
  (studentId, lessonId) => {
    return studentId;
  }

const deleteLesson =
  (studentId, lessonId) => {
    return studentId;
  }

const calculateAvailableLessons =
  (studentId, instrumentId, length) => {
    return studentId;
  }

 */