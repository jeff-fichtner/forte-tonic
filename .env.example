# Environment Configuration for Local Development
# Copy this file to .env and fill in your actual values
# 
# For production deployment configuration, see:
# - .env.render.template (Render dashboard variables)
# - docs/ENVIRONMENT_VARIABLES.md (complete reference)

NODE_ENV=development
PORT=3000

# Google Service Account Configuration
# Use the same variable names as production for consistency
GOOGLE_SERVICE_ACCOUNT_EMAIL=your-service-account@your-project.iam.gserviceaccount.com
GOOGLE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# Google Sheets Configuration
# Replace with your actual development/test spreadsheet ID (NOT production!)
WORKING_SPREADSHEET_ID=your-development-spreadsheet-id-here

# Operator Authentication
# Email address for operator access (not added to any public examples for security)
OPERATOR_EMAIL=your-operator-email@domain.com

# Email Configuration (if using email features)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Note: For production deployment, see .env.render.template and docs/ENVIRONMENT_VARIABLES.md
# Note: GOOGLE_APPS_SCRIPT_ID is only needed for legacy GAS features
# Note: Use development/test spreadsheet IDs locally - never production data
